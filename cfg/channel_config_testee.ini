[pubrole]				# channel名，要以“[”开头，”]“结尾
mc_port= 10080			# 组播端口
type=1				# 类型，1为只接收，2为只发送，3为接收发送		# 本地接收组播消息的网卡ip
local_port = 10080			# 本地接收组播消息的端口

[vehiclecontrol]	
mc_port= 10081			# 组播端口	
type=3				# 类型，1为只接收，2为只发送，3为接收发送	
local_port = 10081			# 本地接收组播消息的端口

[prepare]	
mc_port= 10082			# 组播端口	
type=3				# 类型，1为只接收，2为只发送，3为接收发送	
local_port = 10082			# 本地接收组播消息的端口

[notify]	
mc_port= 10083			# 组播端口	
type=1				# 类型，1为只接收，2为只发送，3为接收发送	
local_port = 10083			# 本地接收组播消息的端口	

[heartbeat]			# 心跳组(必须设置的组)，daemon的type设置为1，其他客户端设置为2
mc_port= 10084			# 组播端口
type=2				# 类型，1为只接收，2为只发送，3为接收发送
local_port = 10084			# 本地接收组播消息的端口